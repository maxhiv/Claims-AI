{
  "name": "ai-scheduler-monorepo",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r --parallel build",
    "lint": "pnpm -r --parallel lint",
    "start": "pnpm -r --parallel start"
  },
  "dependencies": {
    "@sendgrid/mail": "^8.1.6",
    "lucide-react": "^0.544.0",
    "openai": "^5.23.1"
  }
}
